apiVersion: 1

datasources:
  - name: 'MongoDB'
    type: 'alandave-mongodb-datasource'
    access: proxy
    isDefault: true
    orgId: 1
    version: 1
    editable: true
    jsonData:
      connectionString: 'mongodb://mongo:27017'
      database: 'sampledb'
      timeoutSeconds: 30
